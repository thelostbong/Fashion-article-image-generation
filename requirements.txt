# Fashion Article Image Generation — Python dependencies
# Package names verified against the imports in Article_img_generation.py.
# Pin exact versions with `pip freeze` once you have a working GPU environment.

# Install PyTorch separately, matched to your CUDA version, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
torch

transformers>=4.40.0
diffusers>=0.27.0
accelerate
sentencepiece

pandas
openpyxl
pillow
numpy
